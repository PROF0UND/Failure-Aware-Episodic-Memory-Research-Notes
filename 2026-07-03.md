- Ran [[PentestGPT]] against [[scanner-service]] 
- [[PentestGPT]] piggybacked on my Claude code CLI:
	- Used claude-opus-4-8 for reasoning and
	- claude-haiku-4-5-20251001 for parsing
- Produced an insane amount of tool calls insanely fast
- It found a flag after looking for it in the actual directory 
- It took Claude almost 100+ unrestricted steps to "capture" the flag suggesting that the previous 10 step limit will be unable to solve
- ==Idea:== Producing commands in batches?
- Next Steps: 
	1. Instead of checking if the agent is able to find the flags, try to see if the agent produces tool calls based on the reflections?
	2. Set down metrics to grade the pre existing run on.
		1. Repeated action rate
			1. for each run, count how many tool calls are near-duplicates of a previous tool call in the same session.
		2. Technique diversity
		3. Post-failure pivot time
			1. after a confirmed failure event, how many turns before the agent tries something meaningfully different
	3. Detecting semantic similarity in AI responses involves converting text into numerical vectors (embeddings) and measuring the mathematical distance between them. However, this is something that changes the agent's quality not the memory structure.
	4. Test FAEM script with a Claude code setup similar to [[PentestGPT]]