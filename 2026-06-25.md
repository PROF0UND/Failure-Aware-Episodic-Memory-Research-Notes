### FAEM Game Plan

#### ✅ Done

- FAEM memory architecture implemented (`faem.py`)
- Baseline (`no_memory.py`) implemented
- LangGraph agent loop with `InMemorySaver` checkpointer
- `evaluate_step` failure detection with app-level phrases and SQLSTATE patterns
- `infer_phase` with `think=True` fix
- Reflection logging verified in JSONL
- `log_reader.py` working
- KORP Terminal challenge working with metadata
- TimeKORP Dockerfile patched (Debian Buster archive fix) and building
- TimeKORP `metadata.json` written
- Dead-end episode schema designed (`episode_type`, `technique_class`, `failure_count`, `dead_end_scope`)
- Dead-end injection strategy decided (last `HumanMessage` with `id="dead_end_memory"`)

---

#### 🔧 Implement Next (in order)

**1. Dead-end episode logic** ← most important, core FAEM contribution

- Add new fields to `FailureMemoryEntry` schema
- Implement `_maybe_promote_dead_end()` with `DEAD_END_THRESHOLD = 3`
- Write separate `_generate_dead_end_reflection()` prompt
- Wire `id="dead_end_memory"` injection into turn loop

**2. `--debug` flag**

- Add debug node to LangGraph graph
- Color-coded context dump to terminal per turn
- Plain text log to `logs/.../debug_{timestamp}.txt`

**3. Select second benchmark challenge**

- Flag Command still unexplored — check it before committing
- Need working Docker build + `metadata.json`

---

#### 🧪 Validate Before Full Runs

- Dry run (5–6 iterations) on TimeKORP with FAEM
- Confirm at least one reflection entry appears in JSONL
- Use `--debug` to visually verify dead-end memory lands last in context
- Confirm `technique_class` is being assigned by model
- Confirm `dead_end_memory` HumanMessage appears after conversation history

---

#### 📊 Full Experiment Runs

- Lock metric definitions before seeing results:
    - Task success rate
    - Repeated failure count
    - Recovery efficiency (steps between failure and pivot)
    - Token usage
- Run matrix: TimeKORP + Challenge2 × Baseline + FAEM, n=3 per cell
- Switch to `gemma4:31b` for recorded runs
- Raise `max_iterations` to 15+

---

#### ✍️ Write-up

- Voyager paper review (pending)
- Draft paper sections: related work, architecture, results

---

**Immediate next action** is the dead-end episode logic — everything else depends on it being in place before the dry run. Want a Claude Code prompt for that now?