- Finished the first implementation of the testing environment.
- First test still pending
- Removed the raw-replay baseline
	- during the implementation of the agent, I realized that [[LangGraph]] already has a running context memory feature 
- What context does it record.
`channel_values['messages']` is the literal list of LangChain message objects (System/Human/AI/Tool), each carrying its `id`, `content`, and type-specific fields (`tool_call_id` on ToolMessage, etc.), plus a thin layer of LangGraph bookkeeping (`channel_versions`, `checkpoint_id`, `ts`) that's about versioning, not content. You can say concretely "it records exactly the message history, nothing summarized or transformed" — and you've verified that with a printed example, not just docs.


- More control over [[LangGraph]]'s memory?
	- Possible to expand?
- Figure out the APIs
- Figure out the details of [[LangGraph]] to see if memory can be manipulated.
- 